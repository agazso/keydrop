export const encryptSym = (secret: string, message: string): string => {
    return message;
};

export const decryptSym = (secret: string, message: string): string => {
    return message;
};

export const encryptWithPublicKey = (publicKey: string, message: string): string => {
    return message;
};

export const decryptWithPrivateKey = (privateKey: string, message: string): string => {
    return message;
};
